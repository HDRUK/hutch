---
- name: Set up VM
  hosts: tre_server
  tasks:
    - name: Install pip, venv and build-essential
      become: true
      ansible.builtin.apt:
        name:
          - python3-pip
          - python3-venv
          - build-essential
        update_cache: true

- ansible.builtin.import_playbook: install-docker.yml
